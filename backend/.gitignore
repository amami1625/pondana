# ファイルの無視設定について詳しくは以下を参照:
# https://help.github.com/articles/ignoring-files
#
# エディタやOSが生成する一時ファイルを無視する場合は、グローバル設定を使用してください:
#   git config --global core.excludesfile '~/.gitignore_global'

# Bundlerの設定ファイルを無視
/.bundle

# 環境変数ファイルを無視（テンプレートを除く）
/.env*
!/.env*.erb

# ログファイルと一時ファイルを無視
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# PIDファイルを無視（ディレクトリは保持）
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# ストレージ（開発環境でアップロードされたファイルとSQLiteデータベース）を無視
/storage/*
!/storage/.keep
/tmp/storage/*
!/tmp/storage/
!/tmp/storage/.keep

/public/assets

# 認証情報の復号化に使用するマスターキーを無視
/config/master.key

# テストカバレッジレポートを無視
/coverage
