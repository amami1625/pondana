# ===========================================
# 開発環境用 環境変数テンプレート
# ===========================================
# このファイルをコピーして .env ファイルを作成してください
# cp .env.example .env

# -------------------------------------------
# フロントエンド (Next.js)
# -------------------------------------------

# Next.js のサイトURL
# 開発環境: http://localhost:3001
# 本番環境: https://your-app.vercel.app
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# Rails API への接続URL（Next.js サーバーコンポーネントと API Route から Rails へ）
# 開発環境（Docker Compose使用時）: http://app:3000/api
# 本番環境: https://your-backend.onrender.com/api
API_BASE_URL=http://app:3000/api

# Supabase 接続情報
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# -------------------------------------------
# バックエンド (Rails)
# -------------------------------------------

# フロントエンドのURL（CORS設定用）
# 開発環境: http://localhost:3001
# 本番環境: https://your-app.vercel.app
FRONTEND_URL=http://localhost:3001

# Supabase JWT シークレット（JWT検証用）
SUPABASE_JWT_SECRET=your_supabase_jwt_secret

# Rails 環境
# development | test | production
RAILS_ENV=development

# Rails セッション暗号化キー（本番環境のみ必須）
# 生成方法: cd backend && rails secret
# SECRET_KEY_BASE=your_secret_key_base

# データベース接続URL（本番環境のみ）
# Neon を使用する場合はこちらを設定
# DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# データベースパスワード（DATABASE_URL使用時は不要）
# APP_DATABASE_PASSWORD=your_database_password

# Rails ログ設定（本番環境推奨）
# RAILS_LOG_TO_STDOUT=true
# RAILS_SERVE_STATIC_FILES=true
